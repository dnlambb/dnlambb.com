---
import MenuLinks from "./MenuLinks.astro";

export interface Props {
  language: string;
}
const { language, href } = Astro.props;

let links = {}
let currentHref;
let changeLanguage;

if (language == "English") {
  changeLanguage = "/"
  currentHref = "/es"
  links = {
    home: {
      text: "Home",
      href: "/es",
    },
    about: {
      text: "Sobre",
      href: "/es/about",
    },
    projects: {
      text: "Proyectos",
      href: "/es/projects",
    },
    education: {
      text: "Educación",
      href: "/es/education",
    },
    contact: {
      text: "Contacto",
      href: "/es/contact",
    },
  }
}
else {
  currentHref = "/"
  changeLanguage = "/es"
  links = {
    home: {
      text: "Home",
      href: "/",
    },
    about: {
      text: "About",
      href: "/about",
    },
    projects: {
      text: "Projects",
      href: "/projects",
    },
    education: {
      text: "Education",
      href: "/education",
    },
    contact: {
      text: "Contact",
      href: "/contact",
    },
  }
}
---

<header>
  <nav>
    <a href={currentHref} class="brand-link">
      <img src="/public/assets/lamb.svg" alt="lamb icon with link to home page" />
    </a>
    <a href={changeLanguage}>{language}</a>
    <MenuLinks links={links} />
  </nav>
</header>

<style lang="scss">
  @import "../styles/colors.scss";

  header {
    z-index: 5;
    width: 100%;
    height: fit-content;
    padding: 0 1.25rem;
    border-bottom: 1px solid $primary;

    nav {
      width: 100%;
      height: 100%;
      margin: 0.313rem 0;
      display: flex;
      align-items: center;
      justify-content: space-between;

      .brand-link {
        width: fit-content;
        height: fit-content;
        img {
          height: 2.5rem;
          width: 2.5rem;
          background: $secondary;
          border-radius: 100%;
        }
      }
      a {
        text-decoration: none;
      }
      a:hover {
        color: $primary;
      }
    }
  }
  @media (min-width: 768px) {
    header {
      padding: 0 2.5rem;
      flex-direction: row;
    }
  }
</style>
